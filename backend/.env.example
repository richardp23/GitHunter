# GitHub Personal Access Token (optional but recommended)
# Create at: https://github.com/settings/tokens
# Without this: 60 API requests/hour. With this: 5000 requests/hour
GITHUB_TOKEN=your_token_here

# Redis URL (required for job queue and cache)
REDIS_URL=redis://localhost:6379

# Report cache TTL in seconds (default: 3600 = 1 hour)
REPORT_CACHE_TTL=3600

# Google Gemini API key (for AI analysis; get at https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_api_key_here

# Google Slides API (for presentation generation). Use one of:
# - GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
# - GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...} (inline JSON)
# Create a service account in GCP, enable Google Slides API and Drive API, and download the key.

# Optional: Drive file ID of a Slides template. If set, we clone it then fill; otherwise create blank.
# SLIDES_TEMPLATE_ID=

# Optional: Drive folder ID. New presentations are created here when set (only helps with impersonation; see below).
# SLIDES_DRIVE_FOLDER_ID=

# Optional: Google Workspace user email for domain-wide delegation. When set, the service account acts as this user;
# files are created in their Drive and use their quota (fixes "storage quota exceeded"). Requires a Workspace admin to
# add the service account's Client ID in Admin → Security → API controls → Domain-wide delegation and grant scopes:
# https://www.googleapis.com/auth/drive, https://www.googleapis.com/auth/presentations
# SLIDES_IMPERSONATE_EMAIL=

# Delay in ms before deleting our copy after generation (user gets "Make a copy" link). Default 300000 (5 min). Set 0 to disable.
# SLIDES_CLEANUP_DELAY_MS=300000
