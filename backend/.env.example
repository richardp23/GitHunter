# GitHub Personal Access Token (optional but recommended)
# Create at: https://github.com/settings/tokens
# Without this: 60 API requests/hour. With this: 5000 requests/hour
GITHUB_TOKEN=your_token_here

# Redis URL (required for job queue and cache)
REDIS_URL=redis://localhost:6379

# Report cache TTL in seconds (default: 3600 = 1 hour)
REPORT_CACHE_TTL=3600

# Google Gemini API key (for AI analysis; get at https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_api_key_here

# Google Slides API (for presentation generation). Use OAuth (recommended) or service account:

# OAuth (recommended for personal Gmail; no Workspace needed). See backend/docs/GOOGLE_SLIDES_OAUTH_SETUP.md
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REFRESH_TOKEN=

# Optional: Drive file ID of a Slides template. If set, we clone it then fill; otherwise create blank.
# SLIDES_TEMPLATE_ID=

# Optional: Drive folder ID. When using service account, create in shared folder to avoid quota.
# SLIDES_DRIVE_FOLDER_ID=

# Delay in ms before deleting our copy after generation (user gets "Make a copy" link). Default 300000 (5 min). Set 0 to disable.
# SLIDES_CLEANUP_DELAY_MS=300000

# Supabase (optional): long-term archive for reports. Enterprise portal lists and loads from here; Redis is the cache.
# Create a project at https://supabase.com, then create table archived_reports (see backend/docs/SUPABASE_SETUP.md).
SUPABASE_URL=https://<your-project-ref>.supabase.co
# Use the new secret key (recommended): Project Settings → API → "Publishable and secret API keys" → Secret key (sb_secret_...).
SUPABASE_SECRET_KEY=sb_secret_...
